Using only standard libraries, implements visualisation of the bit structure of
chars and integers in C.

If no command line parameters given, runs set of unit tests for all non-void
functions.

If command line parameters included, checks inputs for validity and if invalid
prints “Input error.”. If both inputs valid and the specified type is either
char or int, prints the bit structure of the given data in groups of a nibble.
